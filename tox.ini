# tox.ini
[tox]
envlist = py310, py39, py38

[testenv]
deps =
    pytest
    flake8

commands =
    pytest --junitxml=junit-{envname}.xml {env:TEST_FILES}
    flake8 mymodule

[testenv:coverage]
deps =
    pytest
    coverage

commands =
    coverage run -m pytest tests
    coverage report
